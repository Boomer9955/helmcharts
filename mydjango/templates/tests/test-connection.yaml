apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "djangosql.fullname" . }}-test-connection"
  labels:
    {{- include "djangosql.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "djangosql.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
