apiVersion: v1
kind: Service
metadata:
  name: {{ include "djangosql.fullname" . }}
  labels:
    {{- include "djangosql.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "djangosql.selectorLabels" . | nindent 4 }}
